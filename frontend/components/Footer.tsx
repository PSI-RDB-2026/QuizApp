import { memo } from "react";
import type { FC } from "react";

interface Props {
  
}

export const Footer: FC<Props> = memo((props) => {
  return (
    <div>
      <h2>Footer</h2>
    </div>
  );
});
